import { CircularProgress } from "@mui/material";

export function ZooLoader() {
  return <CircularProgress size={48} />;
}

export function SmallZooLoader() {
  return <CircularProgress size={24} />;
}
